<adonisControls:AdonisWindow x:Class="SMT.WPF.ProgressWindow"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
                             xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
                             Title="AI 翻译进度"
                             Height="500"
                             Width="500"
                             WindowStartupLocation="CenterOwner"
                             ResizeMode="NoResize"
                             ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Name="ProgressLabel"
               Content="准备中..."
               FontSize="14"
               Margin="0,0,0,10" />

        <ProgressBar Grid.Row="1" Name="ProgressBar"
                     Minimum="0"
                     Maximum="100"
                     Value="0"
                     Height="24" />

        <TextBox Grid.Row="2" Name="CurrentTextTextBox"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 Margin="0,10,0,0"
                 FontSize="13" />

        <Button Grid.Row="3" Name="CancelBtn"
                Content="取消"
                Width="80"
                Height="30"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Click="CancelBtn_OnClick" />
    </Grid>
</adonisControls:AdonisWindow>
